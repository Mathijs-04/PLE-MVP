Warhammer RAG rules assistant (answers Warhammer tabletop rule questions).

Stack: Laravel (Vite + Vue) + Python (AI backend). JS preferred over TS.

Rules:
- Never commit or push without explicit permission.
- After editing Laravel files, run Pint.
- Use Laravel Boost for Laravel documentation.